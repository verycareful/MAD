{
  "name": "mad-explorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MAD Explorer v2 - human-driven walk of a 2D loss landscape to demonstrate the intent of the MAD optimizer.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
